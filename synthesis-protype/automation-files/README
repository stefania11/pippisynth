# How To Run Automation Files

In order to run these files, you have to run them from the previous directory (`synthesis-prototype`)
to make the files paths work. 

Example:

```
$ pwd
.../synthesis-prototype

$ ./automation-files/script-decision-vars.sh
```